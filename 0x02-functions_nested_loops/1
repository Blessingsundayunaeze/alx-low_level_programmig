#include <stdio.h>
#include "main.h"

/**
 * print_alphabet - printing alphabets in lower case
 * Return: Success 1
 */
void print_alphabet(void)
{
	char i;

	for (i = 'a'; i <= 'z'; i++)
	{
		putchar(i);
	}
	putchar('\n');
	return (0);
}
